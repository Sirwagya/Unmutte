# Copy this file to .env and fill in your secrets.
# NEVER commit your real .env to the repo.

# Select the AI provider used by the Netlify Function
# Options: 'gemini' (default), 'nvidia'
AI_PROVIDER=gemini

# Google Gemini (Generative Language API)
GEMINI_API_KEY=your_gemini_api_key_here
# Optional: force a specific Gemini model (e.g., gemini-2.5-flash)
GEMINI_MODEL=

# NVIDIA API (OpenAI-compatible chat completions)
# Get an API key from https://build.nvidia.com/ (Integrate API)
NVIDIA_API_KEY=your_nvidia_api_key_here
# Optional: force a specific NVIDIA model (e.g., nvidia/nemotron-4-9b-instruct)
NVIDIA_MODEL=
NIM_MODEL=
